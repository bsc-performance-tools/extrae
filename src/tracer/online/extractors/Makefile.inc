
EXTRACTORS_INCLUDES = -I$(ONLINE_DIR)/extractors -I$(COMMON_DIR) -I$(BUFFERS_DIR) -I$(TRACER_DIR) -I$(CLOCKS_DIR) -I$(HWC_DIR) -I$(ONLINE_DIR) -I$(STATS_DIR)/MPI `@SYNAPSE_CONFIG@ --be-cflags`
CONTAINERS_INCLUDES = -I$(ONLINE_DIR)/extractors -I$(COMMON_DIR) -I$(BUFFERS_DIR) -I$(TRACER_DIR) -I$(ONLINE_DIR) -I$(HWC_DIR) -I$(STATS_DIR)/MPI `@SYNAPSE_CONFIG@ --cp-cflags`

if HAVE_SPECTRAL
EXTRACTORS_INCLUDES += @SPECTRAL_CXXFLAGS@
CONTAINERS_INCLUDES += @SPECTRAL_CXXFLAGS@
endif

