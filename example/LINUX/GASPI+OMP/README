This example is structured in two directories:

  ld-preload/
    Contains an example using the LD_PRELOAD feature and GASPI shared libraries

	usage:
	cd ld-preload && make
	gaspi_run -m ~/machines ./trace.sh $PWD/write_all_mtt
