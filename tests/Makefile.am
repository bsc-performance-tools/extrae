# Do not need to go into overhead directory. This is not launched from 
# this directory but rather installed later on the target 
SUBDIRS = src functional # overhead

EXTRA_DIST = cross-compile-sizeof event

# Do not stop testing if tests fail within test subdirectories
MAKEFLAGS = -k
